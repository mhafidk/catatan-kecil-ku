import type { PageLoad } from './$types';

export const load: PageLoad = () => {
	return {
		title: 'Petunjuk Install PWA',
		description: 'Cara menginstal aplikasi Catatan Kecil Ku di HP atau Komputer sebagai PWA (Progressive Web App).'
	};
};
